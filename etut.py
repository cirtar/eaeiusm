def add_numbers(x, y):
    sum = x + y
    return sum  # This returns the value of sum to the caller

result = add_numbers(3, 5)  # This will store the returned value in the variable result
print(result)  # Output: 8
